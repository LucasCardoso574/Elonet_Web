---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Casos de Sucesso - Elonet Habitação">
  <Header currentPath={Astro.url.pathname} />
  <main>
    <section class="page-hero">
      <div class="page-hero-gradient-overlay"></div>
      <div class="container">
        <h1 class="page-title">Casos de Sucesso</h1>
        <p class="page-subtitle">Resultados que comprovam nossa expertise e compromisso com a excelência</p>
      </div>
    </section>
    
    <section class="content-section">
      <div class="container">
        <div class="content-wrapper">
          <p class="content-text" data-aos="fade-up">
            Em breve, compartilharemos casos de sucesso que demonstram nossa capacidade de entregar resultados técnicos e estratégicos em processos de FCVS, novação de créditos e regularização fundiária.
          </p>
          <p class="content-text" data-aos="fade-up" data-aos-delay="100">
            Nossa trajetória de mais de 25 anos no mercado, com reconhecida expertise em Administração de Créditos e Recebíveis Imobiliários, FCVS e Regularização Fundiária, consolidou a ELONET como uma empresa reconhecida pelo profissionalismo, precisão técnica e confiabilidade.
          </p>
          <div class="cta-box" data-aos="fade-up" data-aos-delay="200">
            <p class="cta-text">
              Quer conhecer como podemos ajudar sua instituição?
            </p>
            <a href="/contato" class="cta-button">Entre em contato</a>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Layout>

<script>
  // Preload image and remove skeleton when loaded
  (function() {
    const hero = document.querySelector('.page-hero');
    if (!hero) return;
    
    const imageUrl = 'https://plus.unsplash.com/premium_photo-1725408023469-d0659e7f3545?q=80&w=2000&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D';
    const img = new Image();
    img.onload = () => {
      hero.style.backgroundImage = `url('${imageUrl}')`;
      hero.classList.add('loaded');
    };
    img.src = imageUrl;
  })();
</script>

<script is:inline>
  // Scroll suave para links internos
  document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        const href = this.getAttribute('href');
        if (href && href !== '#') {
          e.preventDefault();
          const target = document.querySelector(href);
          if (target) {
            const headerOffset = 80;
            const elementPosition = target.getBoundingClientRect().top;
            const offsetPosition = elementPosition + window.pageYOffset - headerOffset;
            
            window.scrollTo({
              top: offsetPosition,
              behavior: 'smooth'
            });
          }
        }
      });
    });
  });
  
  // Carregar e inicializar AOS de forma segura
  window.addEventListener('load', function() {
    const script = document.createElement('script');
    script.src = 'https://unpkg.com/aos@2.3.1/dist/aos.js';
    script.async = true;
    script.onload = function() {
      if (typeof AOS !== 'undefined') {
        AOS.init({
          duration: 800,
          easing: 'ease-in-out',
          once: true,
          offset: 50,
          disable: window.innerWidth < 768
        });
      }
    };
    script.onerror = function() {
      console.error('Erro ao carregar AOS');
    };
    document.head.appendChild(script);
  });
</script>

<style>
  /* Garantir visibilidade inicial dos elementos */
  [data-aos] {
    opacity: 1 !important;
    visibility: visible !important;
  }
  
  /* Quando AOS inicializar, permitir animação de opacidade */
  body.aos-initialized [data-aos]:not(.aos-animate) {
    opacity: 0;
    transition: opacity 0.8s ease-in-out, transform 0.8s ease-in-out;
  }
  
  body.aos-initialized [data-aos].aos-animate {
    opacity: 1;
  }
  
  .page-hero {
    position: relative;
    padding: var(--spacing-16);
    background-color: var(--color-gray-800);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    overflow: hidden;
  }
  
  .page-hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      90deg,
      var(--color-gray-200) 0%,
      var(--color-gray-100) 50%,
      var(--color-gray-200) 100%
    );
    background-size: 200% 100%;
    animation: skeleton-loading 1.5s ease-in-out infinite;
    z-index: 1;
    transition: opacity 0.5s ease-out;
  }
  
  .page-hero.loaded::after {
    opacity: 0;
    pointer-events: none;
  }
  
  .page-hero-gradient-overlay {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(15, 35, 15, 0.7), rgba(15, 35, 15, 0.5));
    z-index: 2;
    opacity: 0;
    transition: opacity 0.5s ease-out;
    pointer-events: none;
  }
  
  .page-hero.loaded .page-hero-gradient-overlay {
    opacity: 1;
  }
  
  .page-hero .container {
    position: relative;
    z-index: 5;
  }
  
  @keyframes skeleton-loading {
    0% {
      background-position: 200% 0;
    }
    100% {
      background-position: -200% 0;
    }
  }
  
  .page-title {
    font-size: 2.5rem;
    font-weight: var(--font-weight-bold);
    color: white;
    text-align: center;
    margin-bottom: var(--spacing-4);
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }
  
  .page-subtitle {
    font-size: 1.25rem;
    color: white;
    text-align: center;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }
  
  .content-section {
    padding: var(--spacing-16) 0;
    background-color: white;
  }
  
  .content-wrapper {
    max-width: 48rem;
    margin: 0 auto;
  }
  
  .content-text {
    font-size: 1.125rem;
    line-height: 1.75;
    color: var(--color-gray-600);
    margin-bottom: var(--spacing-6);
  }
  
  .cta-box {
    margin-top: var(--spacing-12);
    padding: var(--spacing-8);
    background-color: var(--color-background-off);
    border-radius: var(--radius-xl);
    text-align: center;
  }
  
  .cta-text {
    font-size: 1.125rem;
    line-height: 1.75;
    color: var(--color-gray-700);
    margin-bottom: var(--spacing-6);
  }
  
  .cta-button {
    display: inline-block;
    padding: var(--spacing-4) var(--spacing-8);
    background-color: var(--color-institutional);
    color: white;
    border-radius: var(--radius-lg);
    font-weight: var(--font-weight-bold);
    transition: all var(--transition-base);
  }
  
  .cta-button:hover {
    background-color: var(--color-institutional-hover);
  }
</style>

