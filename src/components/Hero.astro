---
---

<section class="hero">
  <div class="hero-background"></div>
  <div class="container">
    <div class="hero-grid">
      <div class="hero-content" data-aos="fade-up">
        <h1 class="hero-title">
          Consultoria Especializada em <span class="title-highlight">Sistema Financeiro da Habitação</span>
        </h1>
        
        <p class="hero-description" data-aos="fade-up" data-aos-delay="100">
          28 anos de experiência em FCVS, crédito habitacional e regularização fundiária. Atuamos de forma técnica e estratégica para auxiliar instituições na organização de carteiras e viabilização da novação dos créditos.
        </p>
        
        <div class="hero-actions" data-aos="fade-up" data-aos-delay="200">
          <a href="https://wa.me/5511999999999?text=Olá,%20gostaria%20de%20falar%20com%20um%20especialista" class="btn-hero-primary">Falar com um Especialista</a>
          <a href="/solucoes" class="btn-hero-secondary">Conheça nossas soluções</a>
        </div>
      </div>
      
      <div class="hero-visual" data-aos="fade-up" data-aos-delay="100">
        <div class="visual-container">
          <div class="logo-wrapper">
            <img 
              src="/images/logogrande.png" 
              alt="Elonet Habitação - 28 Anos" 
              class="hero-logo"
            />
            <div class="sparkle sparkle-1"></div>
            <div class="sparkle sparkle-2"></div>
            <div class="sparkle sparkle-3"></div>
            <div class="sparkle sparkle-4"></div>
            <div class="sparkle sparkle-5"></div>
            <div class="sparkle sparkle-6"></div>
            <div class="sparkle sparkle-7"></div>
            <div class="sparkle sparkle-8"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    padding-top: 2rem;
    padding-bottom: 6rem;
  }
  
  @media (min-width: 1024px) {
    .hero {
      padding-top: 4rem;
      padding-bottom: 10rem;
    }
  }
  
  .hero-background {
    position: absolute;
    inset: 0;
    z-index: -1;
    background: radial-gradient(ellipse at top right, rgba(34, 197, 94, 0.4), transparent, transparent);
  }
  
  .hero-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-12);
    align-items: start;
  }
  
  @media (min-width: 1024px) {
    .hero-grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--spacing-8);
      align-items: start;
    }
  }
  
  .hero-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  
  .hero-badge {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    margin-bottom: var(--spacing-6);
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-full);
    border: 1px solid var(--color-green-200);
    background-color: var(--color-green-50);
    font-size: 0.875rem;
    font-weight: var(--font-weight-medium);
    color: var(--color-institutional);
  }
  
  .badge-dot {
    display: flex;
    width: 0.5rem;
    height: 0.5rem;
    margin-right: var(--spacing-2);
    border-radius: var(--radius-full);
    background-color: var(--color-primary);
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  
  .hero-title {
    font-size: 2.25rem;
    font-weight: var(--font-weight-extrabold);
    letter-spacing: -0.025em;
    color: var(--color-gray-900);
    margin-bottom: var(--spacing-6);
    line-height: 1.2;
  }
  
  @media (min-width: 640px) {
    .hero-title {
      font-size: 3rem;
    }
  }
  
  @media (min-width: 768px) {
    .hero-title {
      font-size: 3.75rem;
    }
  }
  
  .title-highlight {
    color: var(--color-institutional);
  }
  
  .hero-description {
    margin-bottom: var(--spacing-8);
    max-width: 32rem;
    font-size: 1.125rem;
    color: var(--color-gray-600);
    line-height: 1.75;
  }
  
  @media (min-width: 640px) {
    .hero-description {
      font-size: 1.25rem;
    }
  }
  
  .hero-actions {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);
    margin-bottom: var(--spacing-10);
  }
  
  @media (min-width: 640px) {
    .hero-actions {
      flex-direction: row;
    }
  }
  
  .btn-hero-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.875rem 2rem;
    border-radius: var(--radius-lg);
    background-color: var(--color-institutional);
    color: white;
    font-size: 1rem;
    font-weight: var(--font-weight-bold);
    text-decoration: none;
    transition: all var(--transition-base);
  }
  
  .btn-hero-primary:hover {
    background-color: var(--color-institutional-hover);
  }
  
  .btn-hero-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.875rem 2rem;
    border-radius: var(--radius-lg);
    border: 2px solid var(--color-institutional);
    background-color: transparent;
    color: var(--color-institutional);
    font-size: 1rem;
    font-weight: var(--font-weight-bold);
    text-decoration: none;
    transition: all var(--transition-base);
  }
  
  .btn-hero-secondary:hover {
    background-color: var(--color-green-50);
  }
  
  .hero-features {
    display: flex;
    align-items: center;
    gap: var(--spacing-6);
    font-size: 0.875rem;
    font-weight: var(--font-weight-medium);
    color: var(--color-gray-500);
  }
  
  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
  }
  
  .feature-item .material-symbols-outlined {
    color: var(--color-institutional);
    font-size: 1.25rem;
  }
  
  .hero-visual {
    position: relative;
    width: 100%;
    max-width: 32rem;
    margin-left: auto;
  }
  
  @media (min-width: 1024px) {
    .hero-visual {
      max-width: none;
    }
  }
  
  .visual-container {
    position: relative;
    width: 100%;
    overflow: visible;
    display: flex;
    align-items: flex-start;
    justify-content: center;
    padding: 0;
  }
  
  .logo-wrapper {
    position: relative;
    width: 100%;
    max-width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .hero-logo {
    width: 100%;
    max-width: 100%;
    height: auto;
    display: block;
    object-fit: contain;
    position: relative;
    z-index: 1;
    animation: logoEntrance 1.2s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
    opacity: 0;
    transform: scale(0.8);
  }
  
  .sparkle {
    position: absolute;
    width: 5px;
    height: 5px;
    background-color: rgba(255, 255, 255, 1);
    border-radius: 50%;
    z-index: 2;
    pointer-events: none;
    box-shadow: 0 0 8px rgba(255, 255, 255, 1), 0 0 16px rgba(255, 255, 255, 0.8), 0 0 24px rgba(255, 255, 255, 0.5);
    opacity: 0;
  }
  
  .sparkle-1 {
    top: 15%;
    left: 20%;
    animation: sparkleFade 2s ease-in-out infinite;
    animation-delay: 0.5s;
  }
  
  .sparkle-2 {
    top: 35%;
    left: 65%;
    animation: sparkleFade 2.2s ease-in-out infinite;
    animation-delay: 1s;
  }
  
  .sparkle-3 {
    top: 55%;
    left: 30%;
    animation: sparkleFade 2.5s ease-in-out infinite;
    animation-delay: 0.3s;
  }
  
  .sparkle-4 {
    top: 25%;
    left: 75%;
    animation: sparkleFade 2.1s ease-in-out infinite;
    animation-delay: 1.2s;
  }
  
  .sparkle-5 {
    top: 70%;
    left: 45%;
    animation: sparkleFade 2.3s ease-in-out infinite;
    animation-delay: 0.8s;
  }
  
  .sparkle-6 {
    top: 45%;
    left: 15%;
    animation: sparkleFade 2.4s ease-in-out infinite;
    animation-delay: 1.5s;
  }
  
  .sparkle-7 {
    top: 60%;
    left: 80%;
    animation: sparkleFade 2.2s ease-in-out infinite;
    animation-delay: 0.4s;
  }
  
  .sparkle-8 {
    top: 80%;
    left: 55%;
    animation: sparkleFade 2.6s ease-in-out infinite;
    animation-delay: 1.1s;
  }
  
  @keyframes logoEntrance {
    0% {
      opacity: 0;
      transform: scale(0.8);
    }
    100% {
      opacity: 1;
      transform: scale(1);
    }
  }
  
  @keyframes sparkleFade {
    0%, 100% {
      opacity: 0;
      transform: scale(0);
    }
    15% {
      opacity: 1;
      transform: scale(1.2);
    }
    85% {
      opacity: 1;
      transform: scale(1.2);
    }
  }
</style>

